const {app,BrowserWindow}=require("electron");const path=require("path");let w;function create(){w=new BrowserWindow({width:1400,height:900,minWidth:1000,minHeight:700,webPreferences:{contextIsolation:true,nodeIntegration:false},icon:path.join(__dirname,"images","icon-512.png"),autoHideMenuBar:true});w.loadFile(path.join(__dirname,"index.html"))}app.whenReady().then(create);app.on("window-all-closed",()=>{if(process.platform!=="darwin")app.quit()});